export interface Product {
  name: string;
  description: string;
  price: number;
  category: string;
  primaryImage: string;
  secondaryImage: string;
}

export interface Post {
  name: string;
  message: string;
  time: string;
}